<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.ort.visualizar">

    <application
        android:allowBackup="true"
        android:icon="@drawable/visualizar_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/visualizar_logo"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.VisualizAR">
        <activity android:name="edu.ort.visualizar.activities.MainActivity" />
        <activity android:name="edu.ort.visualizar.activities.LoginActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name="edu.ort.visualizar.activities.SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>